apply plugin: 'com.android.library'

android {
    compileSdkVersion 27
    buildToolsVersion '27.0.2'

    defaultConfig {
        minSdkVersion 14
        targetSdkVersion 27
    }
}

dependencies {
    compile project(path: ':mvp-base')
    compile 'com.android.support:appcompat-v7:27.0.2'
}
